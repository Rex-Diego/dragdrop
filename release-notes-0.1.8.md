# DragDrop 0.1.8

- 优化设置分组与每个动作的说明，移除同 Markdown 文件内的别名双链选项，旧配置自动迁移为块嵌入。
- 修复 Surface Pen 拖动 Canvas 连接点、四边和四角缩放手柄，保留原生连线、取消与保存流程。
- 普通笔尖命中缩放手柄时平移画布；落笔时按住侧键才调整卡片大小。连接点仍可直接拖动连线。
- 保留普通鼠标、输入控件和跨窗口拖放行为。

验证：lint、类型检查、197 项自动化测试和生产构建通过。用户已确认此前设置与 Canvas 控件问题解决；最新侧键缩放规则仍需实体设备复核。此版本不包含新的 iPad 手指/Pencil 映射。
